import { Component } from '@angular/core';
import { AppComponent } from './app.component';
import {
  Routes,
  RouterModule,
  CanLoad,
  CanActivate,
  RouterLink,
} from '@angular/router';

export const routes: Routes = [];
